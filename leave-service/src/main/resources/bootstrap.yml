spring:
  application:
    name: leave-service
  profiles:
    active: dev-mac
  cloud:
    nacos:
      config:
        file-extension: yml
        server-addr: single.nacos.cn:8848
        username: nacos
        password: 1qazxsw2
        namespace: DEV
        group: leave
        enabled: false
#logging:
#  level:
#    root: info